<UserControl x:Class="Pos_Client.etc.usercontrol.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Pos_Client.etc.usercontrol">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <!-- 이미지 버튼 -->
        <Button Grid.Row="0" x:Name="menu_img" Background="Yellow" Width="180" Height="130" Margin="0"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="15*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20*" />
                    <RowDefinition Height="15*" />
                </Grid.RowDefinitions>

                <!-- 상품명 -->
                <TextBlock Grid.Row="0" x:Name="menu_name" TextWrapping="Wrap"
                           Text="터진 콜라" Margin="3, 5, 3, 3" FontSize="12" />
                <TextBlock Grid.Row="1" x:Name="menu_info" TextWrapping="Wrap"
                           Text="탄산때문에 터진 제로 코카콜라" Margin="3" FontSize="8" />
            </Grid>

            <!-- 수량 버튼 -->
            <local:QuantityButton Grid.Column="1" x:Name="quantity_btn"
                                  Margin="2, 10, 2, 5" />
        </Grid>
    </Grid>
</UserControl>
